
adaptation_lib.basic_colors = {}

adaptation_lib.items = {}
adaptation_lib.lists = {}
adaptation_lib.groups = {}

adaptation_lib.mods = {}

adaptation_lib.player = {
    formspec_inv = "list[current_player;main;1.5,3;8,4;]",
    --[[
    attach_player = function (player_name)
      end,
    detach_player = function (player_name)
      end,
    set_animation = function (player_name, animation, frames)
      end,
    --]]
  }

